# Example Usage

```lua
	-- some code snippet
        require("path.to.mrs_minute").mrs_minute(s, 300, 300, 1600, 60)
	-- OR --
	local widget = require("path.to.mrs_minute")
	widget.mrs_minute(s,w,h,x_cordinate,y_cordinate)
	-- some code snippet
```
